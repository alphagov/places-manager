<h2>New Business support scheme</h2>
<%= semantic_form_for(@scheme, :url => admin_business_support_schemes_path) do |f| %>

  <%= render partial: 'form', locals: { f: f } %>

  <%= f.buttons do %>
    <%= f.commit_button "Create Business Support", :button_html => {:class => 'btn btn-primary'} %>
  <% end %>
<% end %>

<% content_for :page_title, "Create new business support scheme" %>
