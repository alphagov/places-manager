<div class="inner">
  <%= semantic_form_for(@service, :url => admin_service_path(@service), :multipart => true) do |f| %>
    <%= f.inputs :name, :slug %>
    <%= f.inputs do %>
      <%= f.input :new_data_file, :as => :file %>
    <% end %>
    <%= f.buttons %>
  <% end %>
</div>
